<h1>商品一覧</h1>
<% @items.each do |item| %>
  <%= link_to public_item_path(item.id) do %>
    <%= attachment_image_tag item, :image, size: "300x300" %>
  <% end %>
  <%= item.name %>
  <%= item.price %>
<% end %>