<%= attachment_image_tag @item, :image, size: "300x300" %>

<%= @item.name %>
<%= @item.introduction %>
¥<%= @item.price %>(税込)

<%= form_with model: @item, url: 'public_cart_items_path' do |f| %>
  <%= f.collection_select :name, @item, :id, :name %>
  <%= f.submit 'カートに入れる' %>
<% end %>

<!-- url:'パス'→指定したURLにPOST（新規データ送信）する  -->
